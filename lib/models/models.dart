class ChartData {
  String title;
  double value;

  ChartData({
    this.title,
    this.value,
  });
}

class UploadFile {
  String name;
  String data;

  UploadFile({
    this.name,
    this.data,
  });
}
